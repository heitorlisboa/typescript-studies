// enum é a palavra-chave para a declaração de uma enumeration, que serve para declarar CONSTANTES que serão utilizadas em diferentes partes da aplicação
// A enum conta os valores em ordem crescente a partir de 0 automaticamente, mas também é possível declarar os valores diretamente, como no código abaixo
export enum DiasDaSemana {
  DOMINGO = 0,
  SEGUNDA = 1,
  TERCA = 2,
  QUARTA = 3,
  QUINTA = 4,
  SEXTA = 5,
  SABADO = 6,
}
